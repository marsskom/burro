version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/migrations/sql/queries"
    schema: "internal/migrations/sql/schema"
    gen:
      go:
        out: "internal/database"
        overrides:
          - go_type: "github.com/google/uuid.UUID"
            db_type: "text"

          - go_type: "bool"
            db_type: "integer"
